export class User {
    constructor(id, firstName, lastName, city, age) 
    {
        this._userId = id
        this._userFirstName = firstName;
        this._userLastName = lastName
        this._userCity = city;
        this._userAge = age
    }
}